<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="fofa.store.mapper.MemberMapper">
	
	<resultMap type="Member" id="MemberMap">
		<id column="member_id" property="memberId"/>
		<result column="password" property="password"/>
		<result column="birth" property="birthday"/>
		<result column="email" property="email"/>
		<result column="gender" property="gender"/>
		<result column="image_src" property="profileImg"/>
		
	</resultMap>
	
	<insert id="insert" parameterType="Member">
	INSERT INTO member_tb (member_id, password, birth, email, gender, image_src)
 	VALUES (#{memberId}, #{password}, #{birthday}, #{email}, #{gender}, #{profileImg})	
	</insert>
	
	<update id="update" parameterType="Member">
	UPDATE member_tb
	SET member_id = #{memberId}, #{password}, #{birthday}, #{email}, #{gender}, #{image_src}
	WHERE member_id = #{memberId}	
	</update>
	
	<delete id="delete" parameterType="Member">
	DELETE
	FROM member_tb
	WHERE member_id = #{memberId}
	</delete>
	
	<select id="select" parameterType="String" resultMap="MemberMap">
	select member_id, password, birthday, email, gender, profileImg
	FROM member_tb
	WHERE member_id = #{memberId]
	
	</select>
	
	
	</mapper>
