<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="fofa.store.mapper.ImageMapper">
	
	<resultMap type="Image" id="ImageMap">
		<id column="photo_id" property="imageId"/>
		<result column="review_id" property="categoryId"/>
		<result column="file_name" property="filename"/>
	</resultMap>
	
	<insert id="insertMember" parameterType="Member">
	INSERT INTO member_tb (member_id, password, birthday, email, gender, profileImg)
 	VALUES (#{memberId}, #{password}, #{birthday}, #{email}, #{gender}, #{profileImg})	
	</insert>
	
	<update id="updateMember" parameterType="Member">
	UPDATE member_tb
	SET member_id = #{memberId}, #{password}, #{birthday}, #{email}, #{gender}, #{profileImg}
	WHERE member_id = #{memberId}	
	</update>
	
	<delete id="deleteMember" parameterType="Member">
	DELETE
	FROM member_tb
	WHERE member_id = #{memberId}
	</delete>
	
	<select id="selectMember" parameterType="String" resultMap="MemberMap">
	select member_id, password, birthday, email, gender, profileImg
	FROM member_tb
	WHERE member_id = #{memberId]
	
	</select>
	
	
	</mapper>